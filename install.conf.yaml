- clean: ['~']
- link:
    ~/.vimrc: vimrc
    ~/.joker: joker.edn
    # ~/.config/fish/config.fish:
    #   create: true
    #   path: config.fish
    ~/.lein/profiles.clj:
      create: true
      path: profiles.clj
    ~/.spacemacs: .spacemacs
    # ~/.emacs.d/private/user:
    #   create: true
    #   path: spacemacs-user/
- shell:
  - [git submodule update --init --recursive, Installing/updating submodules]
