- clean: ['~']
- link:
    ~/.vimrc: vimrc
    ~/.joker: joker.edn
    ~/.config/ranger:
      create: true
      path: ranger/
    ~/.lein/profiles.clj:
      create: true
      path: profiles.clj
    ~/.spacemacs: .spacemacs
- shell:
  - [git submodule update --init --recursive, Installing/updating submodules]
